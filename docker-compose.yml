services:
  transcriber:
    build:
      context: .
      args:
        - API_KEY=${API_KEY}
    container_name: gemini-transcriber
    ports:
      - "8080:80"
    restart: unless-stopped
    environment:
      - API_KEY=${API_KEY}